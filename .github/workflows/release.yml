name: Release cropper binaries

on:
  release:
    types: [created]

jobs:
  releases-image-crop:
    name: Release Go Binary
    runs-on: ubuntu-latest
    steps:
      - name: Build project
        run: make build
